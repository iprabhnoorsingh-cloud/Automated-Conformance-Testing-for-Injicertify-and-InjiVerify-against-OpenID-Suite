# Config

Placeholder for shared configuration used by future milestones (e.g.
benchmark gate definitions, test-rig endpoints, OpenID conformance suite
configs). Nothing is implemented here yet — this directory exists to keep
that configuration out of application code once it lands.

No secrets should ever be committed to this directory.
